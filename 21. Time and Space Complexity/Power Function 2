// Recursion
// Power Function 2


public static int power(int a, int n) {
    if(n == 0) {
        return 1;
    }

    int halfPowerSq = power2(a, n/2) * power2(a, n/2);

    if(n%2 != 0) {   // a is odd
        return a * halfPowerSq;
    }

    return halfPowerSq;
}

// Time Complexity: O(n)